name: Python

on: workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]

    steps:
      - uses: actions/checkout@v3

      # 파이썬 환경 설정
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python-version }}

      # 파이썬 버전 출력
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"

      # 출력을 파일에 저장
      - name: Save output to file
        run: python -c "print('Output') > /home/ftsa6tm/ac/artifact.txt"
      
      # 아티팩트 업로드
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: /home/ftsa6tm/ac/artifact.txt
name: Python

on: workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]

    steps:
      - uses: actions/checkout@v3

      # 파이썬 환경 설정
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python-version }}

      # 파이썬 버전 출력
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"

      # 출력을 파일에 저장
      - name: Save output to file
        run: python -c "print('Output') > artifact.txt"
      
      # 아티팩트 업로드
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: artifact.txt
